{% if frisbee_data.message %}
    <div class="alert alert-warning">
        {{ frisbee_data.message }}
        <button type="button" class="close" data-dismiss="alert">×</button>
    </div>
{% else %}
    <div class="buttons">
        <div class="pull-right">
            <a href="{{ frisbee_data.url }}" class="btn btn-primary">{{ button_confirm }}</a>
        </div>
    </div>
{% endif %}
