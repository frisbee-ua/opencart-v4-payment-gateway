{% if frisbee_data.message %}
    <div class="d-inline-block pt-2 pd-2 w-100">
        {{ frisbee_data.message }}
        <div class="float-right">
            <button type="button" class="close" data-dismiss="alert">×</button>
        </div>
    </div>
{% else %}
    <div class="d-inline-block pt-2 pd-2 w-100">
        <div class="float-right">
            <a href="{{ frisbee_data.url }}" id="button-confirm" data-loading-text="{{ text_loading }}" class="btn btn-primary">{{ button_confirm }}</a>
        </div>
    </div>
{% endif %}
